// Operadores Ternários ou Condicionais
/* ? .... Se
: .... Senão

const rain = true
const umbrela = rain ? 'Levar guarda-chuva.' : 'Deixar guarda-chuva.'
console.log (umbrela)*/

/* Preciso transferir meu dinheiro.

const balance = true
const isNotBlocket = true
const accountExit = false

const transferOk = balance && isNotBlocket  &&  accountExit ? 'TRANSFERÊNCIA REALIZADA' : "TRANSFERÊNCIA NEGADA"
console.log (transferOk)*/

const notRain = false
const irAPraia = notRain ? 'Eu posso ir à praia hoje.' : 'Eu não irei à praia hoje.'
console.log (irAPraia)
